# Newsletter-Service
A complete server-side implementation of a Newsletter Service using Python + FastAPI + PostgreSQL, including background scheduling to send topic-based emails at configured times via SMTP
